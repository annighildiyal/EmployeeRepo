spring:
 application:
  name: EmployeeApp


 datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ankita
    url: jdbc:mysql://localhost:3306/ankitadb
    username: ankita

 jpa:
    show-sql: true 
    hibernate:
      ddl-auto: update #create table if not there
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

 output:
    ansi:
      enabled: always #give different colour to logger level in console

       